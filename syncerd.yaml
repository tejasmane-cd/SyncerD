git:
  providers:
    - name: source
      type: azuredevops
      owner: clouddrove-sandbox
      project: testing

    - name: codecommit-source
      type: codecommit
      region: us-east-1

  mirrors:
    - name: azure-to-codecommit
      source: source
      destination: codecommit-source

      filters:
        include:
          - "SyncerD-Azure-Source"

      create_missing: true
      adopt: true

  work_dir: ./syncerd-git-cache
  state_path: ./syncerd-git-cache/git-state.json